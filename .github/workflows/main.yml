name: starterwrkflow

on:
  push:
    branches:
    - main
  schedule:
  - cron: "*/5 * * * *"
    


jobs:
  myjob:
    name: display info
    runs-on: ubuntu-latest
    steps:
    - name: task1
      run: uname -a
    - name: task2
      run: hostname
